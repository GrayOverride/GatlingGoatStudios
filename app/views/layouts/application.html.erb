<!DOCTYPE html>
<html>
<head>
  <%= favicon_link_tag 'favicon.ico' %>
  <title>Gatling Goat Studios - Walking up the stairs, kicking down the shark.</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application", :media => "all" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="http://platform.tumblr.com/v1/share.js"></script>
  <div class="pull-right alig-right">
  <% if current_user %>
  <div id="curr"> Logged in as <%= current_user.email %>.</div>
  <%= link_to "Log out", log_out_path %>
  <% else %>
  <div id="curr"><%= link_to 'Login', log_in_path%></div>
  <% end %>
  </div>
</br>
</head>
<body>
  <div class="navstripe" >
    <div class="row-fluid">
    <div class="span2"></div>
    <div class="span8">
    <div class="row-fluid navfont img-clean">
        <div class="span4"><%=link_to image_tag('logo.png'), home_path, :id=>"logo"%></div>
        <div class="span7">
          <br/>
          <br/>
          <ul class="nav nav-pills">
            <li class="<%= 'place disabled' if current_page?(home_path) %>">
              <%= link_to 'Home', home_path%></li>

            <li class="<%= 'place disabled' if current_page?(projects_path) %>">
              <%= link_to 'Projects', projects_path%></li>

            <li class="<%= 'place disabled' if current_page?(about_path) %>">
              <%= link_to 'About us', about_path%></li>

            <li class="<%= 'place disabled' if current_page?(blog_path) %>">
              <%= link_to 'Blog', blog_path%></li>

            <% if current_user %>
            <li class="<%= 'place disabled' if current_page?(messages_path) %>">
              <%= link_to 'Messages', messages_path%></li>
            <% else %>
            <li class="<%= 'place disabled' if current_page?(contact_path) %>">
              <%= link_to 'Contact', contact_path%></li>
            <% end %>
          </ul>
        </div>
      </div>
      </div>
      <div class="span3"></div>
      </div>
    </div>
    <div class="container-fluid">
      <div class="row-fluid ">
        <div class="span2"></div>
        <div class="span8">
          <%= yield %>
          <br />
          <div class="footnote alig-right"><p>
Copyright © 2013 Carl Rockman</p></div>
        </div>
        <div class="span2"></div>
      </div>
    </div>

    <div id="fb-root"></div>
<script>(function(d, s, id) {
  var js, fjs = d.getElementsByTagName(s)[0];
  if (d.getElementById(id)) return;
  js = d.createElement(s); js.id = id;
  js.src = "//connect.facebook.net/en_US/all.js#xfbml=1";
  fjs.parentNode.insertBefore(js, fjs);
}(document, 'script', 'facebook-jssdk'));</script>
    </body>
    </html>

